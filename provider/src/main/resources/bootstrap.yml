

spring:
  application:
    name: lls-provider

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

      config:
        server-addr: 127.0.0.1:8848
        enabled: true
        file-extension: yml

rocketmq:
  name-server: 127.0.0.1:9876
    #生产者
  producer:
    #生产者组名，规定在一个应用里面必须唯一
    group: ${spring.application.name}